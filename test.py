# The script failed because there is `exit` which causes my sandbox script to fail maybe?
# Oh, `exit` inside Fortran source code is `exit` out of the loop.
# But bash thinks I typed exit. Wait, the `exit` command in bash isn't allowed in run_in_bash_session.
